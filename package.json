{
  "name": "pmp-exam-prep-app",
  "version": "1.0.0",
  "description": "PMP Exam Prep Mobile App - Monorepo",
  "private": true,
  "workspaces": [
    "backend/*",
    "mobile",
    "web-admin",
    "shared"
  ],
  "scripts": {
    "install:all": "npm install && npm run install:backend && npm run install:mobile && npm run install:web-admin",
    "install:backend": "cd backend/user-service && npm install && cd ../content-service && npm install && cd ../analytics-service && npm install",
    "install:mobile": "cd mobile && npm install",
    "install:web-admin": "cd web-admin && npm install",
    "dev:backend": "docker-compose up",
    "dev:mobile": "cd mobile && npm start",
    "dev:web-admin": "cd web-admin && npm run dev",
    "build:production": "npm run build --workspace=@pmp-app/shared && npm run build --workspace=@pmp-app/server",
    "migrate:production": "node scripts/migrate-database.js",
    "seed:production": "node scripts/seed-production.js",
    "start:production": "node backend/server/dist/index.js"
  },
  "keywords": [
    "pmp",
    "exam-prep",
    "mobile-app",
    "react-native"
  ],
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "csv-parse": "^6.1.0"
  },
  "allowScripts": {
    "bcrypt@5.1.1": true,
    "esbuild@0.21.5": true
  },
  "devDependencies": {
    "react": "^19.2.3"
  }
}
